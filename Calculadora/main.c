#include <stdio.h>
#include <stdlib.h>

int main()
{
    /* Funcio bool comprova_gramatica(String liniaatractar). Comprova tants parentesis dentrada com de sortida. cerca la coincidencia de tots els caracters dins del vector de caracters possibles*/
    /* Fer verso iterativa per 10 o  */
    printf("Hello world!\n");
    return 0;
}
